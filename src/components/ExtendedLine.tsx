import React from 'react';
import {View} from 'react-native';

const ExtendedLine = (): JSX.Element => {
  return <View style={{borderWidth: 0.5, borderColor: 'gray', width: '100%'}} />;
};

export default ExtendedLine;
